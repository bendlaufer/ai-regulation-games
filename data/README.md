# Data

This directory contains saved simulation outputs used by the analysis notebooks
and paper figures. The files were moved here unchanged from the repository root.

- `simulation_outputs/`: CSV outputs from the simulation and parameter sweeps.

The public reproduction workflow should read these CSVs directly. Do not rerun
the simulation to reproduce the released figures unless a new experiment is
being intentionally generated.

