# Paper Figure Reproduction

The paper figures are available as saved PNG artifacts under `figures/paper/`.
For a release bundle, run:

```sh
python scripts/collect_paper_figures.py
```

This copies the selected saved figures to `outputs/paper_figures/`. It does not
rerun simulations or overwrite the source figure files.

